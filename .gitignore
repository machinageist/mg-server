# Rust build output
/target/

# Editor / OS
.DS_Store
.DS_Store?
._*
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Environment and secrets
.env
.env.*
*.secret
*.secrets
secrets/
secrets.toml
config.local.toml

# Claude Code local settings (project-specific permissions, not shared)
.claude/settings.local.json

# Compressed binary
mg-server.tar.gz

resume-commands

# Project-specific gauntlet output is transient and can retain stale source
# excerpts or operational detail. Keep the reusable pipeline, ignore its output.
/gauntlet-output/
gauntlet-universal/
